# Empty OpenCV3.0 project with cmakelist
This is an empty project with basic image processing libraries imported. To compile simply use cmake (must be installed prior) and the cmakelist:
```Shell
cmake .
make
```
